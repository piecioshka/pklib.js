(function (global) {
    "use strict";

    module("pklib.ui.loader");

    test("exists", function () {
        ok(pklib.ui.loader, "Exists");
    });
    test("show", function () {
        expect(0);
        // TODO: write this!
    });
    test("close", function () {
        expect(0);
        // TODO: write this!
    });

}(this));
