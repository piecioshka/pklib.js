(function (global) {
    "use strict";

    module("pklib.ui.message");

    test("exists", function () {
        ok(pklib.ui.message, "Exists");
    });
    test("show", function () {
        expect(0);
        // TODO: write this!
    });
    test("close", function () {
        expect(0);
        // TODO: write this!
    });

}(this));
