(function (global) {
    "use strict";

    module("pklib.ui.glass");

    test("exists", function () {
        ok(pklib.ui.glass, "Exists");
    });
    test("show", function () {
        expect(0);
        // TODO: write this!
    });
    test("close", function () {
        expect(0);
        // TODO: write this!
    });

}(this));
