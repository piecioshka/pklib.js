pklib.event.add(window, "load", function () {

    module("pklib.ui");

    test("center", function() {
        // TODO: write this
        console.warn("[-] pklib.ui.center");
    });
    test("maximize", function () {
        // TODO: write this
        console.warn("[-] pklib.ui.maximize");
    });
    test("scrollTo", function() {
    	// TODO: write this
        console.warn("[-] pklib.ui.scrollTo");
    });
});